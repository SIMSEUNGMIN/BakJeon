import java.util.Scanner;

public class Problem {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		Scanner scanner = new Scanner(System.in);
		
		int count = 0;
		int stdNum = 0;
		int[] students = null;
		
		count = scanner.nextInt();
		
		for(int i = 0; i < count; i++) {
			stdNum = scanner.nextInt();
			
			students = new int[stdNum];
			
			for(int student = 0; student < stdNum; student++) {
				students[student] = scanner.nextInt();
			}
		
			getAverage(students);
		}
	}
	
	public static void getAverage(int[] students) {
		
		int sum = 0;
		double average = 0;
		
		for(int i = 0; i < students.length; i++) {
			sum += students[i];
		}
		
		average = sum / students.length;
		
		System.out.print(Math.round(average*1000)/1000.0 + "%");
		
	}
}
